import styles from "./Header.module.css";

const Header = () => {
  return (
    <div className={styles.header}>
      <header>
        <h1>TO DO LIST</h1>
      </header>
    </div>
  );
};

export default Header;
